---
import TagLink from "./TagLink.astro";

interface Props {
  tags: string[];
}

const { tags } = Astro.props;
---

<div class="tags">
  {tags.map((tag) => <TagLink tag={tag} />)}
</div>

<style>
  .tags {
    display: flex;
    flex-wrap: wrap;
  }
</style>
